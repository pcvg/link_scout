namespace :link_scout do
  # Rake task to run LinkScout on a single URL
  desc "Run LinkScout"
  task :single do

  end

  # Rake task to run LinkScout on multiple URL's
  task :multiple do
  end
end

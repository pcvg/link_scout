source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in link_scout.gemspec
gemspec

gem 'rack'

group :development, :test do
  gem 'pry'
end

group :test do
  gem 'webmock'
end
